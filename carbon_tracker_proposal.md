# 永續減碳計算器 (Carbon Saving Tracker) 實作計畫書

## 1. 核心概念與效益
此功能旨在將用戶的永續消費行為「具象化」，轉化為直觀的「節省碳排量 (Carbon Savings)」。
*   **效益**：提供明確的數據回饋，大幅提升用戶的成就感與持續使用地圖的動力，並將系統遊戲化。

## 2. 減碳數據推算邏輯 (初步設定)
由於精確計算每家店、每次消費的碳排非常困難，我們將採用**標準化的行為係數**來進行概算：
*   **打卡/評價「無包裝商店」**：預設每次消費減少使用 3 個塑膠袋/包裝 ＝ 節省約 **150g 碳排**。
*   **打卡/評價「蔬食餐廳」**：預設一餐選擇蔬食而非肉食 ＝ 節省約 **800g 碳排**。
*   **打卡/評價「二手/循環商店」**：預設購買一件二手商品 ＝ 節省約 **500g 碳排**。
*   *未來可讓店家自訂其專屬的「單次消費平均減碳值」。*

## 3. 系統實作範圍

### A. 資料庫 (Firestore) 異動
1.  **商店集合 (`merchants`)**：新增 `carbonSavingPerVisit` 欄位 (數值)，允許管理員設定該店單次消費平均節省的碳排量。若未設定，則使用上述的系統預設值。
2.  **用戶集合 (`users`)**：新增 `totalCarbonSaved` 欄位 (數值)，用於儲存該用戶累積的減碳量。
3.  **評價集合 (`reviews`)**：新增 `carbonSaved` 欄位，記錄該次評價所產生的減碳量，避免重複計算或後續公式調整時影響歷史資料。

### B. 介面 (UI) 異動
1.  **個人儀表板 (User Dashboard)**：
    *   在頂部新增一塊醒目的「減碳成就看板」，顯示：`🌿 累計節省碳排：X.XX kg`。
    *   可搭配具象化的文案，例如：「相當於種植了 X 棵樹」或「省下了 X 個塑膠杯」。
2.  **商店詳情頁 (Shop Detail Modal)**：
    *   在商店名稱旁或特點區塊，標示「在這消費平均可省下 XXg 碳排」。
3.  **評價成功提示**：
    *   送出評價後，跳出動畫提示：「感謝評價！您又為地球節省了 XXg 碳排！」。

### C. 邏輯層 (JS) 異動
1.  **提交評價 (`js/ui.js` 的 `handleShopAction`)**：
    *   在寫入 `reviews` 時，同時觸發計算邏輯。
    *   利用 Firebase Transaction 或 Batch，確保評價建立與用戶總量更新的原子性。
2.  **Dashboard 渲染 (`js/ui.js` 的 `showUserDashboard`)**：
    *   讀取用戶的 `totalCarbonSaved` 並渲染在畫面上。

## 4. 未來擴充方向 (Phase 2)
1.  **減碳排行榜 (Leaderboard)**：展示減碳量前 10 名的用戶，增加社群競爭感。
2.  **數位證書分享**：當用戶達到 10kg、50kg 時，產生可分享到 IG/FB 的漂亮數位證書圖片。
3.  **掃碼打卡機制**：與店家合作，在店內放置 QR Code，用戶掃描即可獲得更精準的消費減碳紀錄。

---
**請審閱此計畫書。如果您同意這個架構與推算邏輯，請回覆「通過」，我將開始為您撰寫相關程式碼！**
